# Database Configuration
# IMPORTANT: Use strong passwords in production!
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=mautic
MYSQL_USER=mautic
MYSQL_PASSWORD=

# Mautic Configuration
# Generate a random 32-character string for the secret key
# Run: openssl rand -hex 16
MAUTIC_SECRET_KEY=

# Site URL (no trailing slash)
# For local development: http://localhost:8080
# For production with Cloudflare: https://tywynspiritlink.com
MAUTIC_SITE_URL=http://localhost:8080

# Port Configuration
# For local development: 8080
# For production: 80 (Cloudflare will handle HTTPS on 443)
MAUTIC_PORT=8080

# Email Configuration (Reference Only - Configure in Mautic UI)
# These are for reference when setting up email in Mautic's web interface
# Go to: Settings > Configuration > Email Settings
#
# For Amazon SES API (Recommended for DigitalOcean - SMTP ports blocked):
# Service: Amazon SES
# Region: eu-north-1 (or your region)
# Access Key ID: YOUR_AWS_ACCESS_KEY_ID
# Secret Access Key: YOUR_AWS_SECRET_ACCESS_KEY
#
# For Amazon SES SMTP (if ports 587/465 are available):
# Host: email-smtp.YOUR-REGION.amazonaws.com
# Port: 587
# Username: YOUR_SES_SMTP_USERNAME
# Password: YOUR_SES_SMTP_PASSWORD
# Encryption: TLS

# Admin user must be created through Mautic's web installer
# No environment variables needed - configure during setup wizard

# Time Zone (PHP timezone format)
TZ=Europe/London

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379